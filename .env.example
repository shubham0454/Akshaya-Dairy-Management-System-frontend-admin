# Optional: override API base URL. Must be the BACKEND URL, not the frontend.
# Production default: https://akshaya-dairy-management-system-bac.vercel.app/api
# Leave unset in development to use the Vite proxy (/api -> localhost:3000)
# VITE_API_BASE_URL=https://akshaya-dairy-management-system-bac.vercel.app/api
